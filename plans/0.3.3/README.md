# 0.3.3 计划

**目标**: 稳定当前代码库，为 Architectury 多加载器迁移做准备。
**状态**: P0 已完成。

---

## 总览

```
P0 ── 安全 + Bug + 死代码修复                  ✅ 已完成
P1 ── 运行时健壮性（7 项，立即做，迁移时带走）   ← 当前
P2 ── 代码整洁度（5 项，迁移前做更好）           ← 可选
P3 ── 编辑器 UI 重构（4 项，等 Architectury）    ← 等待
```

---

## 执行策略

### 现在做（P1 → P2）

不影响编辑器 UI 的重构、bugfix、性能优化。迁移 Architectury 时这些代码会一并带走，现在改比迁移后再改少背债。

### 等 Architectury 再做（P3）

涉及编辑器 UI 的大型重构（Timeline Widget 化、多轨道渲染、双向 Schema、相对坐标）。迁移到新架构后编辑器 UI 大概率要重写，现在改浪费。

---

## 依赖关系

```
P0（已完成）
  ↓
P1 ── C2 · G1 · G2 · P1 · P3 · P4 · ⑤
  │     全部是运行时/渲染层改动，零编辑器 UI 影响
  ↓
P2 ── C1 · D1 · D2 · C4 · E2（可选）
  │     编辑器整洁度，迁移前做更好，不做也行
  ↓
P3 ── ② · ① · ③ · ④
        编辑器 UI 重构，等 Architectury 后再做
```
