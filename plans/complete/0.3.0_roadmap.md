# ImmersiveCinematics 0.3.0 路线图

## 三个交付物

```
0.3.0
├── 脚本播放系统 ✅ 已完成
│   ├── JSON 解析 → POJO 数据层
│   ├── 关键帧插值 + 贝塞尔弧长匀速
│   ├── morph 过渡（顺序时序模型）
│   └── CameraManager 生命周期 + ScriptPlayer 调度
│
├── 触发器系统 ❌ 未开始
│   ├── TriggerEngine（服务端双通道调度）
│   ├── 评估器（location/advancement/login/dimension_change/entity_kill/interact/biome/custom/command）
│   ├── SNBT 触发状态存储
│   └── 网络包 4 个（S2CPlay/S2CStop/C2SFinished/登录同步）
│
└── 游戏内编辑器 ❌ 未开始
    ├── CameraEditorScreen + FBO 离屏预览
    ├── 左侧属性面板
    ├── 底部时间轴（冻结区 + 画布 + 播放头 + 标尺 + 轨道行）
    └── 播放控制 + 时间联动
```

## 状态

| 模块 | 类数 | 状态 |
|------|:----:|:----:|
| 脚本系统 | ~25 | ✅ |
| 触发器系统 | ~15 新增 | ❌ 设计已定（`trigger_system_plan_v4.md`） |
| 编辑器 | ~20 新增 | ❌ 设计已定（`plans/ui_plan.md`） |

## 排除项（移入 future_roadmap.md）

- Phase 1.5 区块强加载 + 刷怪
- dissolve crossfade（FBO alpha 溶解过渡）
- 音频轨道实现
- 事件/ModEvent 轨道实现
- 跨维度过渡运镜
