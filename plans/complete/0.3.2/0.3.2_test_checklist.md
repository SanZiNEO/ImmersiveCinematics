# 0.3.2 测试清单

---

## 一、编辑器基础

- [x] 1. F6 打开编辑器 → 四个区域正常渲染（无空白/黑屏）
- [x] 2. 菜单栏按钮可点击（脚本列表/新建/保存）
- [x] 3. 脚本列表显示所有 .json 文件
- [x] 4. 点击脚本 → 加载并在编辑器中展示
- [x] 5. 新建脚本 → 默认 CAMERA + LETTERBOX 双轨道
- [ ] 6. ESC 关闭编辑器 → F6 重开 → 上个脚本仍在

## 二、编辑器交互

- [x] 7. 时间轴 +C 添加 clip → clip 矩形正常显示
- [x] 8. +K 添加关键帧 → 菱形在 clip 内显示
- [x] 9. 拖拽 clip 左右移动 → 平滑、位置正确
- [x] 10. 拖拽 clip 边缘调整大小 → 正常
- [x] 11. 拖拽关键帧 → 平滑、时间正确
- [x] 12. Ctrl+滚轮缩放时间轴 → 刻度变化正常
- [x] 13. 点击时间轴 ruler → 播放头跳转
- [x] 14. 预览区播放/暂停/停止按钮正常

## 三、Letterbox 轨道

- [x] 15. +C 添加 letterbox clip → 编辑器显示 clip
- [x] 16. +K 添加 letterbox 关键帧 → 菱形显示，属性面板显示 aspect_ratio
- [x] 17. 编辑器播放 → 黑边随关键帧 aspect_ratio 变化
- [x] 18. 播放无 letterbox 关键帧 → 黑边不闪/不卡

## 四、Camera 间隙释放

- [x] 19. 脚本只有 CAMERA clip 0-3s + 6-9s → 3-6s 间隙期视角自由转动
- [x] 20. 纯黑边脚本（无 CAMERA 轨道）→ 全程视角不锁
- [x] 21. CAMERA clip 内 → 视角锁定跟随关键帧

## 五、Morph 过渡

- [x] 22. 两段相邻 clip，transition=morph → 前 clip 末尾有紫色矩形 + "morph Xs" 标签
- [x] 23. morph 过渡播放 → 平滑从上一段末帧过渡到下一段首帧
- [x] 24. cut 过渡 → 硬切无过渡动画

## 六、指令

- [ ] 25. `/icinematics play test_gap` → 自己播放
- [x] 26. `/icinematics play test_gap @a` → Tab 补全 `@` 弹出玩家选择器
- [x] 27. `/icinematics stop` → 停止
- [x] 28. `/icinematics reload` → 重载脚本目录

## 七、脚本解析兼容

- [ ] 29. 旧 letterbox 脚本（enabled/aspect_ratio/fade_in/fade_out 无 keyframes）→ 正常解析
- [ ] 30. 新 letterbox 脚本（keyframes + aspect_ratio）→ 正常解析
- [ ] 31. letterbox 关键帧只写 time + aspect_ratio（无 yaw/pitch/roll/fov）→ 不报错

## 八、GUI Scale 自适应

- [x] 32. 视频设置 GUI Scale 1 → F6 → 布局不溢出、按钮可点
- [x] 33. 视频设置 GUI Scale 3 → F6 → 同上
- [x] 34. 拖拽窗口改变大小 → 布局比例维持、面板不消失
