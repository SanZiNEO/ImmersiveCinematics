package com.immersivecinematics.immersive_cinematics.script;

/**
 * 关键帧属性插值类型
 * <p>
 * 关键帧之间为匀速过渡（由 KeyframeInterpolator 处理），
 * 不依赖此枚举进行缓动。枚举仅用于 JSON 校验白名单。
 */
public enum InterpolationType {
    LINEAR
}
